import React from "react";
import "./NavBarStyle.css"

const NavBar = () => {
    return(
        <header className="header">
            Employee Directory
        </header>
    );
}

export default NavBar;